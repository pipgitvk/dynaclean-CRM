export const metadata = {
  title: 'Return Products - Accounts Dashboard',
  description: 'Manage product returns and track their status',
};

export default function ReturnProductsLayout({ children }) {
  return children;
}
